{
  "name": "weekly-planner-webapp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "node scripts/prepare-plans.js",
    "dev": "npm run prebuild && vite",
    "build": "npm run prebuild && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.0",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.0",
    "vite": "^6.1.0"
  }
}
